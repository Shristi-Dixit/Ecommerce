# Environment Variables Template
# Copy this file to server/.env and fill in your values

# MongoDB Connection String
# Get this from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster0
MONGO_URI=mongodb+srv://ecommerce:newPassword@cluster0.rlzt4gv.mongodb.net/?appName=Cluster0

# JWT Secret for token signing
# Keep this secure and unique! Never commit the actual value to git
# Generate with: openssl rand -base64 32
JWT_SECRET=someverysecretkey

# Server Port
# The Express server will listen on this port
PORT=5000

# Optional: Frontend URL (for CORS)
# Update server/index.js if using a different frontend URL
FRONTEND_URL=http://localhost:5173
